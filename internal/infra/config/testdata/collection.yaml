name: Sample
vars:
  base_url: https://example.com
requests:
  - name: Get users
    method: GET
    url: "{{base_url}}/users"
    headers:
      Accept: application/json
    assert:
      status: 200
    extract:
      user_id: $.data.id
