# Lynix

**Lynix** is a TUI-first CLI for APIs: build and run **requests**, **functional checks**, and (soon) **performance benchmarks** — all from the terminal, in a Git-friendly way.

## Goals
- **TUI-first** (no need to memorize commands)
- **Git-friendly** YAML collections & environments
- **CI-friendly** (later: thresholds, JUnit, artifacts)
- **Single-binary** Go tool

## Requirements
- Go 1.22+

## Quick start
```bash
make tidy
make dev
