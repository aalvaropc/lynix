{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/aalvaropc/lynix/schemas/collection.schema.json",
  "title": "Lynix Collection",
  "description": "Schema for Lynix collection YAML files.",
  "type": "object",
  "required": ["name", "requests"],
  "additionalProperties": false,
  "properties": {
    "schema_version": {
      "type": "integer",
      "minimum": 1,
      "default": 1,
      "description": "Schema version for forward compatibility."
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "Collection name."
    },
    "vars": {
      "type": "object",
      "additionalProperties": { "type": "string" },
      "description": "Default variables available to all requests."
    },
    "requests": {
      "type": "array",
      "minItems": 0,
      "items": { "$ref": "#/$defs/request" },
      "description": "Ordered list of HTTP requests."
    }
  },
  "$defs": {
    "request": {
      "type": "object",
      "required": ["name", "method", "url"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1
        },
        "method": {
          "type": "string",
          "enum": ["GET", "POST", "PUT", "PATCH", "DELETE", "HEAD", "OPTIONS"]
        },
        "url": {
          "type": "string",
          "minLength": 1
        },
        "headers": {
          "type": "object",
          "additionalProperties": { "type": "string" }
        },
        "json": {
          "description": "JSON request body (object or array).",
          "type": ["object", "array"]
        },
        "form": {
          "type": "object",
          "additionalProperties": { "type": "string" }
        },
        "raw": {
          "type": "string"
        },
        "tags": {
          "type": "array",
          "items": { "type": "string" }
        },
        "delay_ms": {
          "type": "integer",
          "minimum": 0,
          "description": "Delay in milliseconds before executing this request."
        },
        "timeout_ms": {
          "type": "integer",
          "minimum": 1,
          "description": "Per-request timeout in milliseconds. Overrides the global HTTP client timeout."
        },
        "follow_redirects": {
          "type": "boolean",
          "description": "Whether to follow HTTP redirects. Default true. Set false to stop at the redirect response."
        },
        "assert": { "$ref": "#/$defs/assertions" },
        "extract": {
          "type": "object",
          "additionalProperties": { "type": "string" },
          "description": "Variable extraction: name -> JSONPath expression."
        },
        "extract_headers": {
          "type": "object",
          "additionalProperties": { "type": "string" },
          "description": "Variable extraction from response headers: name -> header name."
        }
      }
    },
    "assertions": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "status": {
          "oneOf": [
            { "type": "integer" },
            { "type": "array", "items": { "type": "integer" }, "minItems": 1 }
          ]
        },
        "max_ms": { "type": "integer" },
        "body": {
          "type": "object",
          "description": "Assertions on the raw response body (any content type).",
          "additionalProperties": false,
          "minProperties": 1,
          "properties": {
            "eq": { "type": "string" },
            "contains": { "type": "string" },
            "not_contains": { "type": "string" },
            "matches": { "type": "string" },
            "not_matches": { "type": "string" }
          }
        },
        "jsonpath": {
          "type": "object",
          "description": "JSONPath assertions. Each key must be a valid JSONPath expression (e.g. \"$.data\", \"$.users[0].id\") evaluated against the response body.",
          "additionalProperties": { "$ref": "#/$defs/value_assertion" }
        },
        "headers": {
          "type": "object",
          "description": "Response header assertions. Keys are header names (case-insensitive).",
          "additionalProperties": { "$ref": "#/$defs/value_assertion" }
        },
        "schema": { "type": "string" },
        "schema_inline": { "type": "object" }
      }
    },
    "value_assertion": {
      "type": "object",
      "additionalProperties": false,
      "minProperties": 1,
      "properties": {
        "exists": { "type": "boolean" },
        "eq": { "type": "string" },
        "contains": { "type": "string" },
        "matches": { "type": "string" },
        "not_matches": { "type": "string" },
        "gt": { "type": "number" },
        "lt": { "type": "number" },
        "gte": { "type": "number" },
        "lte": { "type": "number" },
        "not_eq": { "type": "string" },
        "not_contains": { "type": "string" },
        "len": { "type": "integer", "minimum": 0 }
      }
    }
  }
}
